syntax = "proto3";

package auth;

service AuthService {
  rpc Authenticate (Authenticaion) returns (UserMessage) {}
}

message Authenticaion {
  string Authenticaion = 1;
}

message UserMessage {
  string id = 1;
  string email = 2;
  string password = 3;
  repeated string roles = 4;
}